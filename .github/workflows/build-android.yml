name: Build Android Release APK

on:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      # Step 1: Install Dependencies
      - name: Install Frontend Dependencies
        working-directory: frontend
        run: npm install --legacy-peer-deps

      # Step 2: Build Web Application Bundle
      - name: Build Web Application
        working-directory: frontend
        run: npm run build

      # Step 3: Ensure Native Android Wrapper Exists & Sync
      - name: Ensure Capacitor Android Platform
        working-directory: frontend
        run: |
          if [ ! -f "android/settings.gradle" ]; then
            echo "Scaffolding missing Capacitor Android platform..."
            rm -rf android
            npx @capacitor/cli add android
          fi
          npx @capacitor/cli sync android

      # Step 4: Set Executable Permissions
      - name: Grant Gradle Permissions
        working-directory: frontend/android
        run: chmod +x gradlew

      # Step 5: Build Debug Signed APK (Always Installable on Android Devices)
      - name: Build Installable Android APK
        working-directory: frontend/android
        run: ./gradlew assembleDebug

      # Step 6: Upload Output Installable Binary
      - name: Upload Signed Release APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: frontend/android/app/build/outputs/apk/debug/app-debug.apk
