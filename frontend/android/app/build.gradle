defaultConfig {
    applicationId "com.neelkanth.accountbook"
    minSdkVersion 22
    targetSdkVersion 34
    versionCode 2          // Increment by +1 on every new release (e.g., 1 -> 2)
    versionName "1.0.1"    // Release Version Name
}
